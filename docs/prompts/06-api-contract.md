Define the API contract for the MVP.



For each page, list the endpoints (or server actions) required:

\- Home aggregates

\- Stock detail (ticker + chart series + KPIs + ratings + activity + related articles)

\- Article detail (content + tickers + sources + disclosures)

\- Entity detail (holdings + trades + performance)

\- Activity feed (filters/pagination)

\- Watchlist + alerts CRUD



Include:

\- Request/response types in TypeScript

\- Pagination strategy

\- Caching strategy (ISR/route cache) and invalidation triggers

\- Error handling patterns

Then implement these routes/actions in Next.js.


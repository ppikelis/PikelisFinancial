Define a UI design system for a finance app:

\- Typography scale, spacing, layout grid

\- Colors as tokens (don’t pick brand colors; use semantic tokens)

\- Key reusable components:

&nbsp; - TickerBadge, PriceChange, Sparkline, OHLC chart container

&nbsp; - KPIGrid, RatingPill, SourceLink, DisclosureBox

&nbsp; - ActivityRow (with entity, action, amount, date, confidence)

&nbsp; - ArticleCard, ArticleHeader, InlineDataWidget

&nbsp; - WatchlistButton, AlertRuleBuilder

\- For each component: props interface in TypeScript + usage example.



Use Tailwind + shadcn/ui patterns.


Implement:

\- Ticker search (prefix + fuzzy) and a /search route

\- A basic screener page /screener with filters:

&nbsp; - recent insider buys

&nbsp; - recent congress buys

&nbsp; - most mentions in articles last 7 days

\- Backend queries with Prisma + indexes

\- UI with filter sidebar and results table

Use mock data if needed but wire to DB if already available.

